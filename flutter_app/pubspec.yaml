name: ai_auto_trader
description: AI Auto Trader — Flutter dashboard for Android and Web.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  cloud_functions: ^5.0.0
  firebase_messaging: ^15.0.0

  flutter_riverpod: ^2.5.0
  go_router: ^14.0.0

  fl_chart: ^0.68.0
  shimmer: ^3.0.0
  timeago: ^3.6.0
  intl: ^0.19.0
  google_fonts: ^6.2.1
  google_sign_in: ^7.2.0
  google_sign_in_web: ^1.1.3
  share_plus: ^9.0.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
